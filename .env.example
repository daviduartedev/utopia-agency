# Copie para .env e preencha localmente. O arquivo .env não é versionado.
#
# URL pública (sem barra final) — usada no build para og:image / Twitter Card (prévia no Google e redes).
# Open Graph / Twitter: imagem e URL canónicos estão em index.html (utopia.app.br).
#
# Supabase — formulário de contato (tabela contact_submissions).
# SQL: supabase/contact_submissions.sql
# VITE_SUPABASE_URL=https://xxxx.supabase.co
# VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
